{
  "name": "@dhc/digitalhome-cloud-core",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "DigitalHome.Cloud Core — norm-agnostic T-Box + per-norm C-Box SHACL profiles (DH-SPEC-200)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@zazuko/env-node": "^1.0.0",
    "n3": "^1.17.0",
    "rdf-validate-shacl": "^0.6.0",
    "vitest": "^3.0.0"
  }
}
